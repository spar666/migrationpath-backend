/**
 * OBSOLETE — SAFE TO DELETE THIS FILE.
 *
 * This was a temporary scratch script used to confirm that TypeORM could build
 * entity metadata after the `ConsultationBooking.scheduler_event_id`
 * DataTypeNotSupportedError.
 *
 * That check now lives permanently, and properly, as a test:
 *   src/database/entity-metadata.spec.ts
 *
 * The body is emptied rather than the file removed only because the sandbox
 * could not delete it. It imports nothing and executes nothing.
 */

export {};
